name: "Apache Iceberg"
description: "REST catalog for Apache Iceberg table format"
long_description: "Usage:  docker [OPTIONS] COMMAND"
version: "1.0.0"
author: "System"
type: install
root: true
order: 20
hidden: false
installed: false
check_command: "docker ps --filter name=iceberg-rest --format '{{.Names}}' | grep -q iceberg-rest"
dependencies:
  - docker
tags:
  - docker
  - data-lake
  - catalog
supported_os:
  - kali
  - debian
  - ubuntu
