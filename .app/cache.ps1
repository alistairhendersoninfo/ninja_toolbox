# TODO: Windows PowerShell implementation
# cache.ps1 — PowerShell cache builder (equivalent of cache.py)
#
# Reads the same YAML metadata files and writes to the same SQLite schema
# (.cache/menu.db) as the Python version.
#
# Dependencies:
#   - PSSQLite module (Install-Module PSSQLite) or System.Data.SQLite (NuGet)
#   - powershell-yaml module (Install-Module powershell-yaml)

Write-Host "PowerShell cache builder is not yet implemented." -ForegroundColor Yellow
