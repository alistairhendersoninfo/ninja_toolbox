# TODO: Windows PowerShell implementation
# ninja-rebuild.ps1 — PowerShell equivalent of ninja-rebuild.sh
# Rebuilds .cache/menu.db from YAML metadata files using PSSQLite

Write-Host "PowerShell ninja-rebuild is not yet implemented." -ForegroundColor Yellow
