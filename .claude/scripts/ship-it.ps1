# TODO: Windows PowerShell implementation
# ship-it.ps1 — PowerShell equivalent of ship-it.sh
# Full PR lifecycle: sync main -> branch -> stage -> commit -> push -> PR -> merge -> cleanup

Write-Host "PowerShell ship-it is not yet implemented." -ForegroundColor Yellow
